[package]
name = "thekingofspace/vision"
version = "1.1.2"
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
description = "A declarative instance library for Luau. Describe a tree as a table, stage data into it, and create it when you are ready."
authors = ["raikin"]

[dependencies]
